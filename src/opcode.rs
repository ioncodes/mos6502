#[derive(PartialEq, Debug)]
pub enum Opcode {
    SEI,
    CLD,
    LDA,
    STA,
    LDX,
    TXS,
    BPL,
    BRK,
    AND,
    ORA,
    BIT,
    ASL,
    CLC,
    BMI,
    JSR,
    ROL,
    Unknown,
}
